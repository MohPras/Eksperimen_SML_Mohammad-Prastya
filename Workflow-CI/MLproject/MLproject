name: cs-mlflow

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      csv_url: {type: str, default: "Workflow-CI/MLproject/netflix_preprocessing.csv"}
    command: >
      python modeling_tuning.py --csv_url {csv_url}